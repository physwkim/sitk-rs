warning: unresolved link to `functor`
  |
  = note: the link appears in this line:
          
          Both policies are the two faces of the [`functor`] module's pixel-functor
                                                  ^^^^^^^^^
  = note: no item named `functor` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`
  = note: `#[warn(rustdoc::broken_intra_doc_links)]` on by default

warning: unresolved link to `unary_minus`
  |
  = note: the link appears in this line:
          
          [`unary_minus`] is the unary face of the same pixel-type-compute policy
           ^^^^^^^^^^^^^
  = note: no item named `unary_minus` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `FilterError::RequiresSignedPixelType`
  |
  = note: the link appears in this line:
          
          [`FilterError::RequiresSignedPixelType`] in place of the C++ compile-time
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `FilterError` in scope

warning: unresolved link to `ImageIo`
  |
  = note: the link appears in this line:
          
          Every format is an [`ImageIo`] implementor sitting in one [`registry`];
                              ^^^^^^^^^
  = note: no item named `ImageIo` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `registry`
  |
  = note: the link appears in this line:
          
          Every format is an [`ImageIo`] implementor sitting in one [`registry`];
                                                                     ^^^^^^^^^^
  = note: no item named `registry` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageFileReader`
  |
  = note: the link appears in this line:
          
          [`ImageFileReader`] and [`ImageFileWriter`] ask the registry which IO
           ^^^^^^^^^^^^^^^^^
  = note: no item named `ImageFileReader` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageFileWriter`
  |
  = note: the link appears in this line:
          
          [`ImageFileReader`] and [`ImageFileWriter`] ask the registry which IO
                                   ^^^^^^^^^^^^^^^^^
  = note: no item named `ImageFileWriter` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `image_io`
  |
  = note: the link appears in this line:
          
          one registry entry — no dispatch to extend. See [`image_io`] for the probe
                                                             ^^^^^^^^^^
  = note: no item named `image_io` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `read_image`
  |
  = note: the link appears in this line:
          
          [`read_image`] and [`write_image`] are the procedural shorthand SimpleITK
           ^^^^^^^^^^^^
  = note: no item named `read_image` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `write_image`
  |
  = note: the link appears in this line:
          
          [`read_image`] and [`write_image`] are the procedural shorthand SimpleITK
                              ^^^^^^^^^^^^^
  = note: no item named `write_image` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `meta_image`
  |
  = note: the link appears in this line:
          
          * [`meta_image`] — MetaImage (`.mha`, `.mhd` + `.raw` / `.zraw`), ITK's
             ^^^^^^^^^^^^
  = note: no item named `meta_image` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `nrrd`
  |
  = note: the link appears in this line:
          
          * [`nrrd`] — NRRD (`.nrrd` / `.nhdr`), `raw` and `gzip` encodings, both of
             ^^^^^^
  = note: no item named `nrrd` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `nifti`
  |
  = note: the link appears in this line:
          
          * [`nifti`] — NIfTI-1 (`.nii`, `.hdr` + `.img`, and the `.gz` spelling of
             ^^^^^^^
  = note: no item named `nifti` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageFileWriter::set_use_compression`
  |
  = note: the link appears in this line:
          
          Compression on write is opt-in through [`ImageFileWriter::set_use_compression`]
                                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageFileWriter` in scope

warning: unresolved link to `write_image_with`
  |
  = note: the link appears in this line:
          
          or [`write_image_with`] — except for NIfTI, where the `.gz` extension alone
              ^^^^^^^^^^^^^^^^^^
  = note: no item named `write_image_with` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `compression`
  |
  = note: the link appears in this line:
          
          decides, exactly as upstream's `nifti_is_gzfile` does. [`compression`] owns
                                                                  ^^^^^^^^^^^^^
  = note: no item named `compression` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `transform_io`
  |
  = note: the link appears in this line:
          
          (`.tfm` / `.txt`, see [`transform_io`]), HDF5 (`.h5` / `.hdf5`, see
                                 ^^^^^^^^^^^^^^
  = note: no item named `transform_io` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `transform_hdf5`
  |
  = note: the link appears in this line:
          
          [`transform_hdf5`]), and MATLAB Level-4 (`.mat`, see [`transform_matlab`]).
           ^^^^^^^^^^^^^^^^
  = note: no item named `transform_hdf5` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `transform_matlab`
  |
  = note: the link appears in this line:
          
          [`transform_hdf5`]), and MATLAB Level-4 (`.mat`, see [`transform_matlab`]).
                                                                ^^^^^^^^^^^^^^^^^^
  = note: no item named `transform_matlab` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageRegistrationMethod`
  |
  = note: the link appears in this line:
          
          [`ImageRegistrationMethod`] driver.
           ^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `MeanSquaresMetric`
  |
  = note: the link appears in this line:
          
          images: the **mean-squares** metric ([`MeanSquaresMetric`]) sampled over the
                                                ^^^^^^^^^^^^^^^^^^^
  = note: no item named `MeanSquaresMetric` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `GradientDescentOptimizer`
  |
  = note: the link appears in this line:
          
          **gradient-descent** optimizer ([`GradientDescentOptimizer`] with a fixed or
                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `GradientDescentOptimizer` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `RegularStepGradientDescentOptimizer`
  |
  = note: the link appears in this line:
          
          estimated rate, or [`RegularStepGradientDescentOptimizer`], which halves its
                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `RegularStepGradientDescentOptimizer` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `MeanSquaresMetric`
  |
  = note: the link appears in this line:
          
          The metric is selectable: mean squares ([`MeanSquaresMetric`]) by default, or
                                                   ^^^^^^^^^^^^^^^^^^^
  = note: no item named `MeanSquaresMetric` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `MattesMutualInformationMetric`
  |
  = note: the link appears in this line:
          
          **Mattes mutual information** ([`MattesMutualInformationMetric`],
                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `MattesMutualInformationMetric` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageRegistrationMethod::set_metric_as_mattes_mutual_information`
  |
  = note: the link appears in this line:
          
          [`set_metric_as_mattes_mutual_information`](ImageRegistrationMethod::set_metric_as_mattes_mutual_information).
                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `ImageRegistrationMethod::set_optimizer_scales_from_jacobian`
  |
  = note: the link appears in this line:
          
          [`ImageRegistrationMethod::set_optimizer_scales_from_jacobian`] and
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `ImageRegistrationMethod::set_optimizer_scales_from_index_shift`
  |
  = note: the link appears in this line:
          
          [`ImageRegistrationMethod::set_optimizer_scales_from_index_shift`]:
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `ImageRegistrationMethod::set_shrink_factors_per_level`
  |
  = note: the link appears in this line:
          
          ImageRegistrationMethod::set_shrink_factors_per_level
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `ImageRegistrationMethod::set_smoothing_sigmas_per_level`
  |
  = note: the link appears in this line:
          
          ImageRegistrationMethod::set_smoothing_sigmas_per_level
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `ImageRegistrationMethod::set_optimizer_as_regular_step_gradient_descent_estimated`
  |
  = note: the link appears in this line:
          
          ImageRegistrationMethod::set_optimizer_as_regular_step_gradient_descent_estimated
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `MetricBackend`
  |
  = note: the link appears in this line:
          
          The metric's per-sample reduction is isolated behind [`MetricBackend`]; the
                                                                ^^^^^^^^^^^^^^^
  = note: no item named `MetricBackend` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `CpuBackend`
  |
  = note: the link appears in this line:
          
          shipped [`CpuBackend`] runs on the host. A backend implementing the same trait
                   ^^^^^^^^^^^^
  = note: no item named `CpuBackend` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ImageRegistrationMethod::set_metric_backend`
  |
  = note: the link appears in this line:
          
          drops in via [`ImageRegistrationMethod::set_metric_backend`] — no change to
                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ImageRegistrationMethod` in scope

warning: unresolved link to `CpuBackend`
  |
  = note: the link appears in this line:
          
          — it falls back to [`CpuBackend`] on every condition that is not a GPU success
                                ^^^^^^^^^^^^
  = note: no item named `CpuBackend` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: unresolved link to `ResampleImageFilter`
  |
  = note: the link appears in this line:
          
          [`ResampleImageFilter`] with nearest-neighbour and linear interpolation —
           ^^^^^^^^^^^^^^^^^^^^^
  = note: no item named `ResampleImageFilter` in scope
  = help: to escape `[` and `]` characters, add '\' before them like `\[` or `\]`

warning: `sitk` (lib doc) generated 36 warnings
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.10s
   Generated /home/stevek/work/sitk-rs/target/doc/sitk/index.html
